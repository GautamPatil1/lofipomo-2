name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # Set to the branch where your React app is located

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      







      


